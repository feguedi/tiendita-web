{{> header }}

{{> navbar }}

<main class="page">
    <section class="clean-block clean-form dark">
        <div class="container">
            <div class="row">
                <div class="col d-flex flex-column">
                    <p class="text-center text-body">{{ mensaje }}</p>
                </div>
            </div>
        </div>
    </section>
</main>
{{ mensaje }}

{{> footer }}